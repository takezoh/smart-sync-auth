{
  "name": "air-sync-auth",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test:build": "esbuild src/oauth.ts src/pcloud.ts --bundle --format=esm --platform=browser --outdir=.test-dist --out-extension:.js=.mjs",
    "test": "npm run test:build && node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "esbuild": "^0.17.19",
    "typescript": "^5.9.3",
    "wrangler": "^3.99.0"
  }
}
